const initState = {
  profile: null,
  profiles: null,
  loading: false
};

function profileReducer(state = initState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default profileReducer;
